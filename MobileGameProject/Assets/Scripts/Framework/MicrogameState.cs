namespace MobileGameTemplate
{
    /// <summary>
    /// The small set of states shared by every microgame.
    /// </summary>
    public enum MicrogameState
    {
        Menu,
        Countdown,
        Playing,
        Success,
        Failure
    }
}
